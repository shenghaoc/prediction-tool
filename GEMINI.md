# Project Instructions

- This project uses **npm** as the package manager.
- DO NOT use `bun`.
- Always run `npm install` to synchronize dependencies.
- Use `npm test` for running unit tests (backed by Vitest).
